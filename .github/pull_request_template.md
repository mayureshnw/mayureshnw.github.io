## Summary

- 

## Publishing checks

- [ ] Frontmatter is complete for any published post (`title`, `description`, `date`, `lastmod`, `draft`, `categories`, `tags`, `topics`, `ShowToc`, `TocOpen`).
- [ ] Local links and asset paths were validated (`python scripts/validate_publishing.py`).
- [ ] I reviewed `.github/new-post-checklist.md` for any new or substantially updated post.

## Validation

- [ ] `python scripts/validate_publishing.py`
- [ ] `hugo --gc --minify`
